source 'http://rubygems.org'

gem 'rails'
gem 'sqlite3-ruby', :require => 'sqlite3'
gem 'devise'
gem 'cancan'
gem 'haml'
gem 'railties'
gem 'jquery-rails'

group :assets do
	gem 'sass-rails'
	gem 'compass-rails'
		
	# digerata's fork fixes a conflict with LESS that is not yet pulled back into the master
	gem 'compass_twitter_bootstrap', :git => 'https://github.com/vwall/compass-twitter-bootstrap'
	
	# Add any compass extensions here
end
